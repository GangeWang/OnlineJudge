version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - HOST_BACKEND_DIR=/Users/gange/OnlineOJ/backend
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./backend:/app